%ol
  - form_answer.versions.each_with_index do |version, i|
    %li= render 'forms/results_table', :caption => version['submitted_at'].to_s, :skip_header => false, :cases => [form_answer.case], :versions => [[i]]
    