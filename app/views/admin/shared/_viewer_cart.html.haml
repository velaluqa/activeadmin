- if cart.empty?
  %p Your viewer cart is currently empty
- else
  %ul
    - cart.each_with_index do |item, index|
      %li
        %a{:href => cart_item_url(item)}= "#{humanize(item[:type].to_s)} #{item[:id]}"
  %a{:href => admin_viewer_cart_start_path} Start Viewer

    