.permissions_matrix{ class: state }
  - Ability::ACTIVITIES.each_pair do |subject, activities|
    - if state != "show" || @role.allows_any?(activities, subject)
      %li{ class: "permission #{subject}" }
        %label.subject=subject
        %div.activities
          - activities.each do |activity|
            = permissions_matrix_activity(@role, subject, activity, disabled: disabled, state: state)
