= label_tag(field['id'], field['label'], :class => 'control-label')
.controls
  = label_tag(field['id'], :class => 'checkbox') do
    - value, fixed_value_hash = fixed_value(field, data_hash)
    = check_box_tag(field['id'], 'true', value, validators_hash(field).merge(fixed_value_hash))
    = "Yes"
    %p.help_block
    